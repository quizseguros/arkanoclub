import type { Metadata } from "next";
import AboutSection from "@/components/AboutSection";

export const metadata: Metadata = {
  title: "Quem somos | Arkano Club",
};

export default function QuemSomosPage() {
  return <AboutSection />;
}
